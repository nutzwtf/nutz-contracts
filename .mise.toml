[tools]
foundry = "1.8.1"    # forge, cast, anvil, chisel (aqua backend, release tarball)
node = "24"          # Active LTS until 2026-10-20; only used by tooling/
pnpm = "11"
uv = "0.11"

[env]
_.file = ".env"         # gitignored; mise exports its variables whenever you are inside the repo
